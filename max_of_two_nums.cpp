#include<stdio.h>
main()
{
	int a,b;
	printf("enter values of a and b");
	scanf("%d\n%d",&a,&b);
	if(a>b)
	printf("the maximum of two nums is%d",a);
	else
	printf("the maximum of two nums is%d",b);
}
